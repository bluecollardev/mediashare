export interface Stats {
  /**
   * Pre-computed number of users.
   */
  usersCount?: number;
  /**
   * Pre-computed number of likes.
   */
  likesCount?: number;
  /**
   * Pre-computed number of shares.
   */
  sharesCount?: number;
}
