import { MediaItem } from '../../../rxjs-api';

export const index = (props: MediaItem[]) => {
  // : (
  //   mediaItems.map((item) => (
  //     <MediaListItemCheckBox
  //       key={`item-${item.key}`}
  //       title={item.key}
  //       description={''}
  //       checked={item.checked}
  //       // image={image}
  //       changeChecked={(bool) => (item.checked = bool)}
  //       onViewDetail={() => {
  //         navigation.navigate(routeConfig.mediaItemDetail.name);
  //       }}
  //     />
  //   ))
  // )
  return <div>)</div>;
};
